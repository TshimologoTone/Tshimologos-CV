<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive CV</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <header>
        <h1>{{Tshimologo Simon Tone}}</h1>
        <p>{{BIPS & PGDip BA(Digital Transformation) Graduate and Software Development Intern}}</p>
    </header>
    <main>
        <section>
            <h2>Experience</h2>
            <ul>
                {{#each experience}}
                <li>{{this}}</li>
                {{/each}}
            </ul>
        </section>
        <section>
            <h2>Education</h2>
            <ul>
                {{#each education}}
                <li>{{this}}</li>
                {{/each}}
            </ul>
        </section>
        <section>
            <h2>Skills</h2>
            <ul>
                {{#each skills}}
                <li>{{this}}</li>
                {{/each}}
            </ul>
        </section>
    </main>
</body>
</html>
